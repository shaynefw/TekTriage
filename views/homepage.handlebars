{{#if loggedIn}}
  <h2>Welcome, {{username}}!</h2>
{{else}}
  <h2>Welcome to Tek Triage</h2>
{{/if}}

{{#each posts}}
  <div>
    <h3>{{title}}</h3>
    <p>Created on {{createdAt}}</p>
    <p>By {{user.username}}</p>
    <a href="/post/{{id}}">View Post</a>
  </div>
{{/each}}